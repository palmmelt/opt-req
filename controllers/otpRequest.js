// Example mongodb ในตัวอย่างจะเป็นการใช้ mongodb นะครับ
const User = require('../models/');

exports.requrestOtp =async()=>{
     /**
   * Check permission component.
   * @param {string} token
   * @param {string} component
   * @returns status, permission || message
   */
    try{

    }catch(err){

    }
} 